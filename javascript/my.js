// 함수 선언
// function : 함수임을 나타내는 예약어
// myfunc : 함수 이름(사용자 지정)
// {} : 중괄호(scope, block) - 코드의 시작과 끝을 알려줌.
// 함수  함수이름(매게변수){ }
function myfunc() {
  alert("함수 호출됨");
}

// 함수 선언과 호출은 다르다
// 선언은 정의, 호출은 실행!
