# Application Name
spring.application.name=countDB

# 1. Server & Thymeleaf Setup
server.port=8080
spring.thymeleaf.cache=false
spring.thymeleaf.prefix=classpath:/templates/
spring.thymeleaf.suffix=.html

# 2. MySQL Connection Setup
# ??: /counter ?? ????(.count)? ??? ???!
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver
spring.datasource.url=jdbc:mysql://localhost:3306/counter?serverTimezone=Asia/Seoul&characterEncoding=UTF-8
spring.datasource.username=root
spring.datasource.password=gkeptm20010030

# 3. JPA / Hibernate Setup
# validate: ?? ? ??(Table)? ???? ????? ???
spring.jpa.hibernate.ddl-auto=validate
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format_sql=true
spring.jpa.properties.hibernate.use_sql_comments=true
spring.jpa.database-platform=org.hibernate.dialect.MySQLDialect

# 4. Logging Setup (SQL ?? ?? ??)
logging.level.org.hibernate.SQL=debug
logging.level.org.hibernate.orm.jdbc.bind=trace
logging.level.org.hibernate.type.description.sql.BasicBinder=trace

# 5. DevTools (?? ?? ? ?? ???)
spring.devtools.restart.enabled=true
spring.devtools.livereload.enabled=true